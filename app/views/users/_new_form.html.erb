<%= form_for(@user, html: { class: "form-horizontal" }) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= render partial: "admin_form_fields", locals: {f: f} %>

  <div class="actions">
    <%= f.submit "Submit", class: "btn btn-primary" %>
    <%# link_to "Delete", @user, method: :delete, class: "btn btn-danger", data: { confirm: "Are you sure you want to delete the user '#{@user.name}'?" } %>
    <%= link_to "Cancel", @user, class: "btn btn-default" %>
  </div>
<% end %>
